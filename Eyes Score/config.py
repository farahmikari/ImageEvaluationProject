"""
config.py
---------
مسؤولية هذا الملف: تجميع كل الثوابت والإعدادات الخاصة بالمشروع في مكان واحد.

لماذا ملف منفصل؟
لأن معرّفات نقاط العين (landmark indices) الخاصة بـ MediaPipe Face Mesh،
وكذلك عتبات (thresholds) حساب النسبة المئوية، هي "بيانات ثابتة" وليست
منطق برمجي. فصلها هنا يجعل تعديلها لاحقًا (مثلاً لو أردنا تغيير نقاط
العين المستخدمة أو معايرة النسبة) لا يتطلب لمس أي كود منطقي في باقي
الملفات، وهذا يخدم مبدأ Single Responsibility ويسهّل الصيانة.
"""

# --------------------------------------------------------------------------
# نقاط العين في MediaPipe Face Mesh (468 نقطة)
# --------------------------------------------------------------------------
# كل عين تحتاج 6 نقاط لحساب EAR بنفس منطق ورقة Soukupová & Čech الأصلية:
# نقطتان أفقيتان (الزوايا) + 4 نقاط عمودية (أعلى وأسفل العين في موضعين).
#
# الترتيب المتبع لكل عين: [P1, P2, P3, P4, P5, P6]
# P1, P4 => زوايا العين (أفقي)
# P2, P3 => الجفن العلوي (عمودي)
# P5, P6 => الجفن السفلي (عمودي)

LEFT_EYE_LANDMARKS = [33, 160, 158, 133, 153, 144]
RIGHT_EYE_LANDMARKS = [362, 385, 387, 263, 373, 380]

# --------------------------------------------------------------------------
# إعدادات MediaPipe Face Mesh
# --------------------------------------------------------------------------
MAX_NUM_FACES = 1
STATIC_IMAGE_MODE = True          # لأننا نعمل على صورة ثابتة وليس فيديو
MIN_DETECTION_CONFIDENCE = 0.5
REFINE_LANDMARKS = True           # يحسّن دقة نقاط العينين والشفاه

# --------------------------------------------------------------------------
# ثوابت تحويل EAR إلى نسبة مئوية
# --------------------------------------------------------------------------
# هذه القيم تمثل الحدود التقريبية لقيمة EAR:
# - EAR_MIN: القيمة عندما تكون العين مغلقة تمامًا (0% انفتاح)
# - EAR_MAX: القيمة عندما تكون العين مفتوحة بالكامل (100% انفتاح)
# هذه القيم قابلة للمعايرة (calibration) حسب البيانات الفعلية إذا لزم الأمر.
EAR_MIN = 0.15
EAR_MAX = 0.35